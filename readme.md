# MiniGames plugin

The minigames plugin is a multimodule project that has paper, common and velocity as modules. When compiling with maven, it builds all 3 modules. To build any module (and depending modules) add -am -pl [modulename] to the command.